[build-system]
requires=["setuptools>=69","wheel"]
build-backend="setuptools.build_meta"

[project]
name="feedbackpro"
version="0.4.0rc1"
description="Offline customer feedback analysis and control system"
readme="README.md"
requires-python=">=3.11"
dependencies=["openpyxl>=3.1,<4"]

[project.optional-dependencies]
ui=["PySide6>=6.7,<7"]
dev=["pytest>=8,<9"]
release=["pyinstaller>=6,<7"]

[project.scripts]
feedbackpro="feedbackpro.cli:main"
feedbackpro-ui="feedbackpro.ui:main"
feedbackpro-gate-a2="feedbackpro.gate_a2:main"
feedbackpro-gate-a3="feedbackpro.gate_a3:main"

[tool.setuptools.packages.find]
where=["src"]

[tool.setuptools.package-data]
feedbackpro=["Main.qml","build_meta.json","qml/*.qml","qml/components/*.qml"]

[tool.pytest.ini_options]
testpaths=["tests"]
addopts="-q"
